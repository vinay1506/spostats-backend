[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && chmod +x node_modules/.bin/* && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 10 